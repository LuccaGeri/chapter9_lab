pwd: Displays the full path of your current working directory.

ls: Lists all files and folders in the current directory.

git init: Initializes a new Git repository in the current folder.

git status: Shows the state of the working directory and the staging area.

git add: Adds file changes to the staging area to prepare them for a commit.

git commit: Saves the staged snapshot to the project history with a descriptive message.